node_modules/
bun.lockb
dist/
*.log
.DS_Store
.env
.env.local
*.tsbuildinfo
coverage/

# bun.lock IS committed (Bun best practice): needed for reproducible
# installs and for `bun audit` to work in CI.

# dev-only helpers used to stage split commits during plugin work
scripts/
